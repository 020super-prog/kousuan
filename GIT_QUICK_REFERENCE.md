# 🚀 Git 快速参考卡片

## 📋 当前状态

```
✅ 仓库: 已初始化
✅ 分支: master
✅ 提交: 68912c2
✅ 标签: v1.0
✅ 状态: clean (无未提交修改)
```

---

## 🏷️ 版本标签验证

### ✅ 已验证的命令

```bash
# 1. 列出所有标签
git tag -l
# 输出: v1.0 ✅

# 2. 查看标签详情
git tag -l -n9
# 输出:
# v1.0  Release v1.0 - Stable Version
#       Features: Practice, Camera, PDF Generation, Error Management, Unit Decomposition ✅

# 3. 查看完整标签信息
git show v1.0
# 显示完整的提交信息和文件变更 ✅

# 4. 查看带标签的提交历史
git log --oneline --decorate
# 输出: 68912c2 (HEAD -> master, tag: v1.0) ✅

# 5. 图形化查看
git log --oneline --graph --all --decorate
# 显示分支和标签关系图 ✅
```

---

## 📊 提交统计

```
提交哈希: 68912c2
分支: master
标签: v1.0
文件数: 147 files
新增代码: 24,214 行
删除代码: 0 行
```

---

## 🎯 常用命令速查

### 查看状态
```bash
git status              # 查看工作区状态
git status -s           # 简短格式
git log --oneline       # 查看提交历史
git log --stat          # 查看文件变更统计
```

### 标签操作
```bash
git tag                 # 列出所有标签
git tag -l "v1.*"       # 列出v1.x版本
git show v1.0           # 查看标签详情
git tag -a v1.1 -m "msg" # 创建新标签
git tag -d v1.0         # 删除本地标签
```

### 分支操作
```bash
git branch              # 查看本地分支
git branch -a           # 查看所有分支
git checkout -b dev     # 创建并切换到dev分支
git checkout master     # 切换到master分支
```

### 日常提交
```bash
git add .               # 添加所有修改
git add file.js         # 添加单个文件
git commit -m "msg"     # 提交
git diff                # 查看未暂存的修改
git diff --cached       # 查看已暂存的修改
```

---

## 🔧 配置信息

```bash
# 当前配置
用户名: Kousuan Developer
邮箱: dev@kousuan.com
分支: master
远程仓库: (未配置)
```

---

## 📦 下一步操作建议

### 1️⃣ 推送到远程仓库（可选）
```bash
# 添加远程仓库
git remote add origin https://github.com/username/kousuan19.git

# 推送代码
git push -u origin master

# 推送标签
git push origin --tags
```

### 2️⃣ 创建开发分支
```bash
# 创建develop分支
git checkout -b develop

# 在develop分支上开发新功能
git add .
git commit -m "feat: 新功能"

# 合并到master
git checkout master
git merge develop
```

### 3️⃣ 发布新版本
```bash
# 修改代码并提交
git add .
git commit -m "feat: 添加新功能"

# 打标签
git tag -a v1.1 -m "Release v1.1"

# 查看标签
git tag -l
```

---

## ⚠️ 注意事项

1. **定期提交**: 每完成一个功能就提交一次
2. **清晰的提交信息**: 使用规范的提交前缀（feat/fix/docs等）
3. **标签管理**: 重要版本用附注标签（-a），临时标签用轻量级标签
4. **分支策略**: master保持稳定，新功能在develop或feature分支开发
5. **备份**: 定期推送到远程仓库备份

---

## 🆘 救命命令

### 撤销操作
```bash
# 撤销工作区修改（危险！）
git checkout -- file.js

# 从暂存区移除
git reset HEAD file.js

# 撤销最后一次提交（保留修改）
git reset --soft HEAD^

# 撤销最后一次提交（丢弃修改，危险！）
git reset --hard HEAD^
```

### 查看历史
```bash
# 查看某个文件的历史
git log -- file.js

# 查看某次提交的详细信息
git show 68912c2

# 查看两次提交之间的差异
git diff HEAD^ HEAD
```

---

## 📚 提交消息规范

```
feat:     新功能
fix:      修复bug
docs:     文档更新
style:    代码格式（不影响功能）
refactor: 重构代码
perf:     性能优化
test:     测试相关
chore:    构建/工具相关
```

**示例**:
```bash
git commit -m "feat: 添加PDF导出功能"
git commit -m "fix: 修复题目显示错误"
git commit -m "docs: 更新README.md"
```

---

## ✅ 验证清单

- [x] Git仓库已初始化
- [x] 用户信息已配置
- [x] 所有文件已提交
- [x] 标签v1.0已创建
- [x] 标签信息完整
- [x] 工作区干净
- [x] .gitignore已配置
- [x] 文档齐全

---

## 🎉 完成！

**小学口算助手 v1.0 已成功版本化！**

所有代码已安全保存在Git版本控制系统中。

需要帮助？查看 `GIT_SETUP_GUIDE.md` 获取详细指南。
